<Window x:Class="DineFlow.WPFApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DineFlow · Quản lý menu"
        Width="1500"
        Height="920"
        MinWidth="1200"
        MinHeight="740"
        WindowStartupLocation="CenterScreen"
        Background="#F4F7FB"
        FontFamily="Segoe UI Variable Text"
        TextOptions.TextFormattingMode="Display"
        UseLayoutRounding="True">
    <Window.Resources>
        <SolidColorBrush x:Key="SidebarSurface" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SidebarBorder" Color="#E2E8F0"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#0866E5"/>
        <SolidColorBrush x:Key="NavTextBrush" Color="#475569"/>

        <Style x:Key="SidebarNavStyle" TargetType="Button">
            <Setter Property="Height" Value="48"/>
            <Setter Property="Margin" Value="12 3"/>
            <Setter Property="Padding" Value="14 0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource NavTextBrush}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="NavBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="NavBorder" Property="Background" Value="#F0F7FF"/>
                                <Setter Property="Foreground" Value="#0B55A0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="NavBorder" Property="Opacity" Value=".78"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="NavBorder" Property="BorderBrush" Value="#0866E5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="NavIconStyle" TargetType="TextBlock">
            <Setter Property="Width" Value="32"/>
            <Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="FontSize" Value="17"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
        </Style>

        <Style x:Key="NavLabelStyle" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="244"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="{StaticResource SidebarSurface}"
                BorderBrush="{StaticResource SidebarBorder}"
                BorderThickness="0 0 1 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="84"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="78"/>
                </Grid.RowDefinitions>

                <Border BorderBrush="{StaticResource SidebarBorder}" BorderThickness="0 0 0 1" Padding="18 0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="38" Height="38" CornerRadius="10" Background="#0866E5">
                            <TextBlock Text="&#xE7FC;" FontFamily="Segoe Fluent Icons" Foreground="White"
                                       FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="11 0 0 0" VerticalAlignment="Center">
                            <TextBlock Text="DineFlow" Foreground="#0F172A" FontWeight="Bold" FontSize="17"/>
                            <TextBlock Text="Quản lý nhà hàng" Foreground="#64748B" FontSize="11" Margin="0 2 0 0"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <StackPanel Margin="0 14 0 20">
                        <Button x:Name="btnOverview" Style="{StaticResource SidebarNavStyle}" Click="btnOverview_Click">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE80F;" Style="{StaticResource NavIconStyle}"/>
                                <TextBlock Grid.Column="1" Text="Tổng quan" Style="{StaticResource NavLabelStyle}"/>
                            </Grid>
                        </Button>
                        <Button x:Name="btnMenu" Style="{StaticResource SidebarNavStyle}" Click="btnMenu_Click">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE8D4;" Style="{StaticResource NavIconStyle}"/>
                                <TextBlock Grid.Column="1" Text="Menu" Style="{StaticResource NavLabelStyle}"/>
                            </Grid>
                        </Button>
                        <Button x:Name="btnOrders" Style="{StaticResource SidebarNavStyle}" Click="btnOrders_Click">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE8A5;" Style="{StaticResource NavIconStyle}"/>
                                <TextBlock Grid.Column="1" Text="Đơn hàng" Style="{StaticResource NavLabelStyle}"/>
                            </Grid>
                        </Button>
                        <Button x:Name="btnBills" Style="{StaticResource SidebarNavStyle}" Click="btnBills_Click">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE8C7;" Style="{StaticResource NavIconStyle}"/>
                                <TextBlock Grid.Column="1" Text="Hóa đơn" Style="{StaticResource NavLabelStyle}"/>
                            </Grid>
                        </Button>
                        <Button x:Name="btnRequests" Style="{StaticResource SidebarNavStyle}" Click="btnRequests_Click">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE8BD;" Style="{StaticResource NavIconStyle}"/>
                                <TextBlock Grid.Column="1" Text="Yêu cầu" Style="{StaticResource NavLabelStyle}"/>
                            </Grid>
                        </Button>
                        <Button x:Name="btnReports" Style="{StaticResource SidebarNavStyle}" Click="btnReports_Click">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE9D2;" Style="{StaticResource NavIconStyle}"/>
                                <TextBlock Grid.Column="1" Text="Báo cáo" Style="{StaticResource NavLabelStyle}"/>
                            </Grid>
                        </Button>
                        <Button x:Name="btnTables" Style="{StaticResource SidebarNavStyle}" Click="btnTables_Click">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE80A;" Style="{StaticResource NavIconStyle}"/>
                                <TextBlock Grid.Column="1" Text="Sơ đồ bàn" Style="{StaticResource NavLabelStyle}"/>
                            </Grid>
                        </Button>
                        <Button x:Name="btnAuth" Style="{StaticResource SidebarNavStyle}" Click="btnAuth_Click">
                            <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="32"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                <TextBlock Text="&#xE72E;" Style="{StaticResource NavIconStyle}"/>
                                <TextBlock Grid.Column="1" Text="Nhân viên &amp; Phân quyền" Style="{StaticResource NavLabelStyle}"/>
                            </Grid>
                        </Button>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2" BorderBrush="{StaticResource SidebarBorder}" BorderThickness="0 1 0 0" Padding="14 0">
                    <Grid>
                        <Grid.ColumnDefinitions><ColumnDefinition Width="38"/><ColumnDefinition Width="*"/><ColumnDefinition Width="36"/></Grid.ColumnDefinitions>
                        <Border Width="34" Height="34" CornerRadius="9" Background="#F0F7FF">
                            <TextBlock x:Name="txtInitials" Text="AD" Foreground="#0866E5" FontWeight="Bold"
                                       FontSize="11" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Grid.Column="1" Margin="9 0 0 0" VerticalAlignment="Center">
                            <TextBlock x:Name="txtCurrentUser" Foreground="#1E293B" FontWeight="SemiBold" FontSize="12" TextTrimming="CharacterEllipsis"/>
                            <TextBlock x:Name="txtCurrentRole" Foreground="#64748B" FontSize="10" Margin="0 2 0 0"/>
                        </StackPanel>
                        <Button Grid.Column="2" ToolTip="Đăng xuất" Click="btnLogout_Click" Foreground="#64748B"
                                Background="Transparent" BorderThickness="0" Cursor="Hand">
                            <TextBlock Text="&#xF3B1;" FontFamily="Segoe Fluent Icons" FontSize="16"/>
                        </Button>
                    </Grid>
                </Border>
            </Grid>
        </Border>

        <ContentControl x:Name="mainContent" Grid.Column="1"/>
    </Grid>
</Window>
