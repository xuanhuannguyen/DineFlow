<UserControl x:Class="DineFlow.WPFApp.Features.Management.Tables.AreaManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Background="Transparent"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Form Area -->
        <Border Grid.Row="0" Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="16" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#E2E8F0" Direction="270" ShadowDepth="4" BlurRadius="10" Opacity="0.5"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Thông tin khu vực" Style="{StaticResource HeaderTextBlock}" Grid.Row="0"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="2*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Area Name -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Tên khu vực" Style="{StaticResource LabelTextBlock}"/>
                        <TextBox x:Name="txtAreaName" Style="{StaticResource ModernTextBox}" MaxLength="50" MinHeight="40"/>
                    </StackPanel>

                    <!-- Description -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Mô tả" Style="{StaticResource LabelTextBlock}"/>
                        <TextBox x:Name="txtDescription" Style="{StaticResource ModernTextBox}" MaxLength="200" MinHeight="40"/>
                    </StackPanel>
                </Grid>

                <!-- Actions -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button x:Name="btnClear" Content="Làm mới" Style="{StaticResource NeutralButton}" MinWidth="100" Click="BtnClear_Click"/>
                    <Button x:Name="btnUpdate" Content="Cập nhật" Style="{StaticResource PrimaryButton}" MinWidth="100" Margin="10,0,0,0" Click="BtnUpdate_Click"/>
                    <Button x:Name="btnCreate" Content="Thêm mới" Style="{StaticResource PrimaryButton}" MinWidth="100" Margin="10,0,0,0" Click="BtnCreate_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- DataGrid Area -->
        <Border Grid.Row="1" Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="16" Padding="20">
            <Border.Effect>
                <DropShadowEffect Color="#E2E8F0" Direction="270" ShadowDepth="4" BlurRadius="10" Opacity="0.5"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <Grid Grid.Row="0" Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Text="Danh sách khu vực" Style="{StaticResource HeaderTextBlock}" VerticalAlignment="Center" Margin="0"/>
                    
                    <Button x:Name="btnReload" Grid.Column="1" Content="Tải lại dữ liệu" Style="{StaticResource OutlineButton}" Click="BtnReload_Click"/>
                </Grid>

                <DataGrid x:Name="dgAreas" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                          SelectionMode="Single" SelectionChanged="DgAreas_SelectionChanged"
                          Background="Transparent" BorderThickness="0" RowBackground="White" AlternatingRowBackground="#FAFAFA"
                          HorizontalGridLinesBrush="{StaticResource BorderGray}" VerticalGridLinesBrush="Transparent"
                          HeadersVisibility="Column" RowHeight="50">
                    
                    <DataGrid.Resources>
                        <!-- Style for Column Headers -->
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F1F5F9"/>
                            <Setter Property="Foreground" Value="{StaticResource Slate700}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,12"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                        </Style>
                        <!-- Style for Cells -->
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Padding="{TemplateBinding Padding}" Background="{TemplateBinding Background}" BorderBrush="Transparent" BorderThickness="0">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{StaticResource LightBlueBackground}"/>
                                    <Setter Property="Foreground" Value="{StaticResource Slate900}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding AreaId}" Width="60"/>
                        <DataGridTextColumn Header="TÊN KHU VỰC" Binding="{Binding AreaName}" Width="200">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                    <Setter Property="Foreground" Value="{StaticResource Slate900}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="MÔ TẢ" Binding="{Binding Description}" Width="*"/>
                        
                        <DataGridTemplateColumn Header="TRẠNG THÁI" Width="140">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="8,4" CornerRadius="12" HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource GreenLightStatus}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Background" Value="#FEF2F2"/> <!-- RedLight -->
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock FontWeight="SemiBold">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Đang hoạt động"/>
                                                    <Setter Property="Foreground" Value="{StaticResource GreenStatus}"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                            <Setter Property="Text" Value="Ngừng HĐ"/>
                                                            <Setter Property="Foreground" Value="{StaticResource RedStatus}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="SỐ BÀN" Binding="{Binding TableCount}" Width="100"/>
                        
                        <DataGridTemplateColumn Header="HÀNH ĐỘNG" Width="160">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Click="BtnGridToggleActive_Click" Padding="10,4">
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource OutlineButton}">
                                                <Setter Property="Content" Value="Ngừng HĐ" />
                                                <Setter Property="Foreground" Value="{StaticResource RedStatus}" />
                                                <Setter Property="BorderBrush" Value="{StaticResource RedStatus}" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Content" Value="Bật HĐ" />
                                                        <Setter Property="Foreground" Value="{StaticResource GreenStatus}" />
                                                        <Setter Property="BorderBrush" Value="{StaticResource GreenStatus}" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</UserControl>
